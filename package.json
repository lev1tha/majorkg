{
  "name": "majorkg",
  "version": "0.1.0",
  "private": true,
  "description": "MAJOR KG — турнирная платформа по CS2: frontend (Next.js) и backend (Express + SQLite)",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace backend & npm run dev --workspace frontend & wait",
    "dev:api": "npm run dev --workspace backend",
    "dev:web": "npm run dev --workspace frontend",
    "build": "npm run build --workspace backend && npm run build --workspace frontend",
    "start": "npm run start --workspace backend & npm run start --workspace frontend & wait",
    "seed": "npm run seed --workspace backend",
    "typecheck": "npm run typecheck --workspace backend && npm run typecheck --workspace frontend"
  },
  "engines": {
    "node": ">=22"
  }
}
